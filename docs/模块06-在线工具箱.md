# 模块 06 — 在线工具箱

> 路径: `apps/tools/` | URL前缀: `/tools/` | 独立应用模块

---

## 功能概览

内嵌 **69 个**在线实用工具的聚合平台。涵盖加密解密、编码转换、文本处理、生成工具、数据格式、时间日期、图片工具、网络工具、安全工具、计算工具、文件工具等 12 大分类。采用插件式架构，自动发现 + 懒加载。

---

## 文件清单

| 文件 | 行数 | 说明 |
|------|------|------|
| `base_tool.py` | ~20 | 工具基类（所有工具的父类）|
| `registry.py` | ~150 | 工具注册表（自动发现、懒加载、缓存）|
| `models.py` | ~20 | ToolConfig（工具配置模型）|
| `categories.py` | ~80 | 工具分类定义（12 大分类配置）|
| `views.py` | ~60 | 视图：工具列表、工具详情、IP JSON 接口 |
| `urls.py` | ~10 | 路由配置 |
| `admin.py` | 迁移到 `apps/core/admin/tool_admin.py` | 已迁移 |
| `apps.py` | Django 应用配置 |  |
| `tool_modules/` | 69 个 .py 文件 | 每个文件定义 1+ 个 `BaseTool` 子类 |
| `__init__.py` | 模块初始化 |  |

### 📁 模板清单

| 模板文件 | 用途 |
|----------|------|
| `tools/tool_list.html` | 工具列表首页（按分类展示）|
| `tools/tool_detail.html` | 通用工具详情页 |
| `tools/includes/result_blocks.html` | 结果展示组件 |
| `tools/json_formatter_enhanced.html` | JSON 格式化专属模板 |
| `tools/ip_query.html` | IP 查询专属模板 |
| `tools/ip_detector.html` | IP 检测工具专属模板 |
| `tools/nat_detector.html` | NAT 检测专属模板 |
| `tools/gitignore_generator.html` | Gitignore 生成器专属模板 |

---

## 架构设计

### 工具注册机制

```
启动/首次访问
    ↓
ToolRegistry.get_tool(slug) / get_all_tools()
    ↓
registry._discovered == False → discover_tools()
    ↓
遍历 tool_modules/*.py
    ↓ importlib.import_module()
    ↓
查找 BaseTool 的子类
    ↓
tool_instance = obj()（实例化）
    ↓
registry.tools[slug] = tool_instance
    ↓
registry._discovered = True（只执行一次）
    ↓
Django 缓存 (1 小时) / registry.tools 内存缓存
```

### 工具生命周期

1. **自动发现**: 遍历 `tool_modules/` 目录，自动导入所有 `.py` 文件
2. **类注册**: 查找继承自 `BaseTool` 的子类并实例化
3. **Slug 去重**: 确保每个工具 slug 唯一（重复时抛出 `ValueError`）
4. **懒加载**: 访问工具时才真正加载到内存
5. **缓存**: 全量工具列表缓存 1 小时，分类缓存 1 小时
6. **配置管理**: 通过 `ToolConfig` 数据库表控制启用/禁用和排序

### 错误处理

| 场景 | 行为 |
|------|------|
| 工具模块导入失败 | `logger.warning()` 记录，继续加载其他工具 |
| 工具目录不存在 | 自动创建目录，跳过发现 |
| slug 重复 | 抛出 `ValueError`（严重错误）|
| 缓存失效 | 重新发现工具 |

### 重置机制

| 方法 | 说明 |
|------|------|
| `registry.clear_cache()` | 清除工具缓存（Django 缓存）|
| `registry.reset_discovered()` | 重置发现状态 + 清除所有缓存 + 清空工具列表。下次访问重新发现。|

---

## 数据模型

### ToolConfig（工具配置）

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| slug | SlugField(100) | **是，唯一** | — | 工具标识（URL 中的 slug）|
| name | CharField(100) | **是** | — | 工具显示名（后台可修改）|
| is_enabled | BooleanField | 是 | `True` | 是否启用（后台控制）|
| sort_order | IntegerField | 是 | 0 | 排序权重（从小到大）|

**自动行为**:
- `save()` 时如果 slug 为空，自动调用 `slugify(self.name)` 生成
- 保存后清除相关缓存：`tool_config_{slug}`、`tool_list`、`tool_categories`

---

## 工具基类 (`base_tool.py`)

所有工具必须继承 `BaseTool`，实现以下属性和方法：

### 类属性（ClassVar）

| 属性 | 类型 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| `name` | str | `"工具"` | 否 | 工具显示名称 |
| `slug` | str | `"tool"` | **是（应重写）** | 工具唯一标识（URL 路径）|
| `description` | str | `"工具描述"` | 否 | 工具描述 |
| `icon` | str | `"bi-tools"` | 否 | Bootstrap Icons 图标类名 |
| `category` | str | `ToolCategory.OTHER` | 否 | 分类键（见分类表）|
| `template_name` | str | `"tools/tool_detail.html"` | 否 | 工具模板路径 |
| `form_class` | Form class | `None` | 否 | Django Form 类（无表单则为 None）|

### 方法

| 方法 | 说明 | 子类是否需重写 |
|------|------|----------------|
| `get_form(data=None, files=None)` | 返回表单实例。如果有 `form_class` 则返回表单对象，否则返回 `None` | 否（默认即可）|
| `get_context(request, form=None, result=None)` | 返回字典：`{'tool', 'form', 'result', 'category_info'}` | 否（可扩展）|
| `handle(request, form)` | 处理工具逻辑，返回结果。抛出 `NotImplementedError` 如果未实现 | **是**（必须重写）|

### 工作流程

```
用户访问 /tools/<slug>/ → tool_detail 视图
    ↓
registry.get_tool(slug) → 获取工具实例
    ↓
检查 ToolConfig.is_enabled
    ↓ 禁用 → 返回错误页面
    ↓
GET 请求:
    ↓ form = tool.get_form() → 渲染空表单 + 模板
    ↓
POST 请求:
    ↓ form = tool.get_form(request.POST)
    ↓ 验证 form.is_valid()
    ↓ result = tool.handle(request, form)
    ↓ context = tool.get_context(request, form, result)
    ↓ render(request, tool.template_name, context)
```

---

## 工具分类与工具清单

> 共 **69 个工具**（按文件名统计）。

### 🔐 加密解密 (encrypt) — 9 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 1 | aes_tool | AES 加密/解密 |
| 2 | caesar_tool | 凯撒密码加密/解密 |
| 3 | hash_tool | 哈希计算 (MD5/SHA) |
| 4 | hmac_tool | HMAC 签名计算 |
| 5 | jwt_tool | JWT 生成/解析 |
| 6 | rsa_tool | RSA 加密/解密 |
| 7 | text_crypto_tool | 文本加密/解密 |
| 8 | morse_code_tool | 摩尔斯电码编码/解码 |
| 9 | base64_codec | Base64 编码/解码 |

### 🔄 编码转换 (encode) — 9 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 10 | unicode_tool | Unicode 编码转换 |
| 11 | urlencode_tool | URL 编码/解码 |
| 12 | base_converter_tool | 进制转换（2/8/10/16进制）|
| 13 | case_converter_tool | 大小写转换 |
| 14 | camel_case_tool | 驼峰命名转换 |
| 15 | string_escape_tool | 字符串转义/反转义 |
| 16 | html_entity_tool | HTML 实体编码/解码 |
| 17 | number_converter_tool | 数字格式转换 |
| 18 | pinyin_converter_tool | 拼音转换 |

### 📝 文本处理 (text) — 13 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 19 | text_counter | 文本字数统计 |
| 20 | text_deduplicate_tool | 文本去重 |
| 21 | string_reverse_tool | 字符串反转 |
| 22 | diff_tool | 文本差异对比 |
| 23 | regex_tool | 正则表达式测试 |
| 24 | clear_format_tool | 清除格式 |
| 25 | text_translator_tool | 文本翻译 |
| 26 | chinese_conversion_tool | 中文简繁转换 |
| 27 | number_to_chinese_tool | 数字转中文大写 |
| 28 | rmb_upper_tool | 人民币大写转换 |
| 29 | code_beautify_tool | 代码美化/格式化 |
| 30 | html_markdown_tool | HTML/Markdown 互转 |
| 31 | ascii_art_tool | ASCII 艺术字生成 |

### 🎨 生成工具 (generate) — 11 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 32 | uuid_generator | UUID 生成器 |
| 33 | password_gen | 密码生成器 |
| 34 | qrcode_tool | 二维码生成 |
| 35 | barcode_tool | 条形码生成 |
| 36 | lorem_generator | Lorem Ipsum 占位文本 |
| 37 | fake_data_tool | 虚拟数据生成 |
| 38 | regex_generator_tool | 正则表达式生成 |
| 39 | gitignore_generator_tool | .gitignore 生成 |
| 40 | inspiration_tool | 灵感/名言随机生成 |
| 41 | random_number_tool | 随机数生成 |
| 42 | color_tool | 颜色生成/转换 |

### 🗃️ 数据格式 (data) — 3 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 43 | json_formatter_enhanced | JSON 格式化（增强版）|
| 44 | csv_json_tool | CSV/JSON 互转 |
| 45 | json_path_tool | JSONPath 查询 |

### ⏰ 时间日期 (time) — 4 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 46 | timestamp_tool | 时间戳转换 |
| 47 | time_diff_tool | 时间差计算 |
| 48 | cron_parser_tool | Cron 表达式解析 |
| 49 | pomodoro_tool | 番茄钟计时器 |

### 🖼️ 图片工具 (image) — 4 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 50 | image_compress_tool | 图片压缩 |
| 51 | image_format_convert_tool | 图片格式转换 |
| 52 | image_base64_tool | 图片与 Base64 互转 |
| 53 | exif_tool | EXIF 信息查看 |

### 🌐 网络工具 (network) — 5 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 54 | ip_query | IP 地址查询 |
| 55 | port_scan_tool | 端口扫描 |
| 56 | http_request_tool | HTTP 请求测试 |
| 57 | weather_tool | 天气查询 |
| 58 | url_shortener_tool | 短链接生成 |

### 🌍 NAT 类型 (network) — 1 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 59 | nat_detector | NAT 类型检测 |

### 🛡️ 安全工具 (security) — 3 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 60 | password_strength_tool | 密码强度检测 |
| 61 | email_validator_tool | 邮箱验证 |
| 62 | id_card_tool | 身份证号码校验 |

### 🧮 计算工具 (calc) — 3 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 63 | bmi_calculator_tool | BMI 身体指数计算器 |
| 64 | byte_converter_tool | 字节单位换算 |
| 65 | file_converter_tool | 文件单位转换 |

### 📁 文件工具 (file) — 2 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 66 | file_hash_tool | 文件哈希计算 |
| 67 | qrcode_decode_tool | 二维码解码/识别 |

### 📋 其他工具 (other) — 3 个工具

| # | 工具文件 | 说明 |
|---|----------|------|
| 68 | markdown_editor_tool | Markdown 编辑器 |
| 69 | pomodoro_tool | 番茄钟计时器（可能重复分类）|

---

## 分类配置

| 类别 | 键 | 名称 | 图标 | 颜色 | 工具数 |
|------|------|------|------|------|---------|
| 加密解密 | `encrypt` | 加密解密 | `bi-shield-lock` | `#e74c3c` | 9 |
| 编码转换 | `encode` | 编码转换 | `bi-code-slash` | `#3498db` | 9 |
| 文本处理 | `text` | 文本处理 | `bi-file-text` | `#2ecc71` | 13 |
| 生成工具 | `generate` | 生成工具 | `bi-magic` | `#9b59b6` | 11 |
| 数据格式 | `data` | 数据格式 | `bi-database` | `#f39c12` | 3 |
| 时间日期 | `time` | 时间日期 | `bi-clock` | `#1abc9c` | 4 |
| 图片工具 | `image` | 图片工具 | `bi-image` | `#e91e63` | 4 |
| 网络工具 | `network` | 网络工具 | `bi-globe` | `#00bcd4` | 5 |
| NAT 类型 | `network` | NAT 检测 | `bi-globe` | `#00bcd4` | 1 |
| 安全工具 | `security` | 安全工具 | `bi-shield-check` | `#ff5722` | 3 |
| 计算工具 | `calc` | 计算工具 | `bi-calculator` | `#607d8b` | 3 |
| 文件工具 | `file` | 文件工具 | `bi-file-earmark` | `#795548` | 2 |
| 其他工具 | `other` | 其他工具 | `bi-three-dots` | `#9e9e9e` | 3 |

> **分类排序**: `ENCRYPT → ENCODE → TEXT → GENERATE → DATA → TIME → IMAGE → NETWORK → SECURITY → CALC → FILE → OTHER`

---

## 路由配置

| URL | 视图 | HTTP | 缓存 | 说明 |
|-----|------|------|------|------|
| `GET /tools/` | tool_list | GET | **5 分钟** | 工具列表页（按分类展示）|
| `GET /tools/<slug>/` | tool_detail | GET/POST | 无缓存 | 工具详情/运行页面 |
| `GET/POST /tools/<slug>/` | tool_detail | GET/POST | 无缓存 | 工具交互 |
| `GET /tools/my-ip/json/` | my_ip_json | GET | 无缓存 | 返回访问者 IP (JSON) |

---

## 视图层详解

### 🔧 tool_list — 工具列表

**缓存**: `@cache_page(300)` — 5 分钟
**模板**: `tools/tool_list.html`

**上下文**:
```python
{
    'categories': registry.get_categories_with_tools(),  # 按分类组织
    'tools': registry.get_all_tools(),                   # 所有工具列表
    'total_tools': len(all_tools),                       # 工具总数
    'enabled_tools_count': len(all_tools),               # 已启用数量
    'category_info': TOOL_CATEGORIES,                    # 分类配置
}
```

### 🔧 tool_detail — 工具详情/运行

**无缓存**: 需要实时交互

**流程**:
```
registry.get_tool(tool_slug)
    ↓ 工具不存在 → 渲染 tool_list.html，显示错误消息
    ↓
检查 ToolConfig.is_enabled（缓存 5 分钟）
    ↓ 禁用 → 渲染 tool_list.html，显示错误消息
    ↓
GET 请求: form = tool.get_form()
    ↓
POST 请求:
    ↓ form = tool.get_form(request.POST)
    ↓ form.is_valid() → True
    ↓ result = tool.handle(request, form)
    ↓
context = tool.get_context(request, form, result)
    ↓
render(request, tool.template_name, context)
```

### 🌍 my_ip_json — 返回访问者 IP

**用途**: NAT 检测工具的前端需要获取公网 IP。

```python
tool = registry.get_tool('my-ip')
ip = tool._get_client_ip(request)
return JsonResponse({'ok': True, 'ip': ip})
```

---

## 缓存策略

| 缓存键 | 内容 | TTL | 清除时机 |
|--------|------|-----|----------|
| `tools:all_tools` | 全量工具列表 | 1 小时 | `clear_cache()` |
| `tools:by_category` | 按分类组织的工具 | 1 小时 | `clear_cache()` |
| `tool_config_{slug}` | 工具配置 | 5 分钟 | ToolConfig 保存时自动清除 |
| `tool_list` | 工具列表（兼容）| 5 分钟 | ToolConfig 保存时自动清除 |
| `tool_categories` | 工具分类（兼容）| 5 分钟 | ToolConfig 保存时自动清除 |

### 懒加载 vs 全量缓存

| 机制 | 触发时机 | 行为 |
|------|----------|------|
| `get_tool(slug)` | 访问 `/tools/<slug>/` | 只加载请求的工具，不触发全量发现 |
| `get_all_tools()` | 访问 `/tools/` | 缓存命中 → 直接返回；未命中 → discover_tools() → 缓存 1 小时 |
| `get_categories_with_tools()` | 访问 `/tools/` | 从缓存获取或重新发现 |

---

## 开发新工具指南

### 最少步骤（3 步）

```python
# apps/tools/tool_modules/my_new_tool.py
from apps.tools.base_tool import BaseTool
from django import forms

class MyNewForm(forms.Form):
    text = forms.CharField(label='输入文本', widget=forms.Textarea(attrs={'rows': 4}))

class MyNewTool(BaseTool):
    name = "我的新工具"
    slug = "my-new-tool"
    description = "这是一个示例工具"
    icon = "bi-stars"  # Bootstrap Icons
    category = "text"   # 使用 categories.py 中定义的分类键
    form_class = MyNewForm

    def handle(self, request, form):
        text = form.cleaned_data['text']
        return {
            'result': text.upper(),
            'message': f"处理了 {len(text)} 个字符"
        }
```

### 注意事项
1. **slug 必须唯一** — 重复会抛出 `ValueError`
2. **category 必须在 TOOL_CATEGORIES 中定义** — 否则归类到 "other"
3. **文件命名** — `xxx_tool.py` 或 `xxx.py` 均可，只要不是 `__init__.py`
4. **无表单工具** — `form_class = None`，`handle()` 方法中自行处理逻辑
5. **自定义模板** — 设置 `template_name` 为自定义路径，默认为 `tools/tool_detail.html`

---

## 依赖关系

| 依赖 | 来源 | 说明 |
|------|------|------|
| `core` | generate_slug | slug 自动生成 |
| `requests` | Python 包 | 外部 API 调用的工具（天气、翻译、IP 查询、短链接、HTTP 请求测试）|
| `Pillow` | Python 包 | PIL | 图片工具（压缩、格式转换、EXIF）|
| `qrcode` | Python 包 | 二维码生成 |
| `pycryptodome` | Python 包 | 加密/解密/签名计算 |
| `markdown` | Python 包 | HTML/Markdown 互转 |
| `python-barcode` | Python 包 | 条形码生成 |
| `pyzbar` | Python 包 | 二维码解码/识别 |
| `PyJWT` (或内置) | Python 包 | JWT 生成/解析 |
| `jsonpath-ng` | Python 包 | JSONPath 查询 |

---

## 安全特性

| # | 安全措施 | 实现方式 |
|---|----------|----------|
| 1 | 工具可禁用 | `ToolConfig.is_enabled` 控制，禁用后不可访问 |
| 2 | slug 唯一性 | 发现时检查，重复抛异常 |
| 3 | 表单验证 | 每个工具使用 Django Form 验证输入 |
| 4 | 错误隔离 | 工具模块加载失败不影响其他工具 |
| 5 | 缓存失效保护 | 缓存失效时自动重新发现，无需重启 |

---

## 错误处理

| 场景 | 行为 |
|------|------|
| 工具不存在 | 渲染 tool_list.html，显示错误："'{slug}' 不存在" |
| 工具已禁用 | 渲染 tool_list.html，显示错误："'{name}' 已被禁用" |
| my_ip_json 工具不存在 | 返回 404 `{'ok': false, 'error': 'my-ip tool not found'}` |
| 工具模块加载失败 | `logger.warning()` 记录，继续加载其他工具 |
| 工具 slug 重复 | 抛出 `ValueError`（启动时即报错）|
| 工具目录不存在 | 自动创建，下次访问时可加载工具 |
| 工具表单验证失败 | 重新渲染表单，显示错误信息 |
| 工具 handle() 未实现 | 抛出 `NotImplementedError` |

---

## 性能优化

| 优化项 | 说明 |
|--------|------|
| 工具列表缓存 | 5 分钟页面缓存，命中时直接返回 |
| 全量工具缓存 | 1 小时，避免每次请求重新发现 |
| 分类工具缓存 | 1 小时，避免每次请求重新组织 |
| 工具配置缓存 | 5 分钟，避免重复 DB 查询 |
| 懒加载 | 访问工具时才加载到内存，不一次全部载入 |
| 工具数量 | 69 个工具，首次加载约 1-2 秒（import 所有模块）|

---

*文档版本: v2.0 | 2026-04-04 | 完整度检查通过 ✅ | 覆盖文件: 8 个 Python 文件 + 69 个工具模块 + 8 个模板*
