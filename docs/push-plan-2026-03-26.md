# 明天一键推送前清单（2026-03-26）

当前状态：`main` 相对 `origin/main` **ahead 22**

## 一、推送前必做（按顺序）

1. 工作区确认无脏改动
```bash
git status --short --branch
```

2. 运行发布门禁（快速）
```bash
# Windows
deploy\test-gate.bat

# Linux/macOS
bash deploy/test-gate.sh
```

3. （可选）全量门禁
```bash
# Windows
deploy\test-gate.bat --full

# Linux/macOS
bash deploy/test-gate.sh --full
```

4. 查看待推提交
```bash
git log --oneline origin/main..HEAD
```

5. 一键推送
```bash
git push origin main
```

## 二、当前待推提交顺序（旧 -> 新）

1. `905ed87` phase-13
2. `04667a8` phase-14
3. `a858d6b` phase-15
4. `8e96be8` phase-16
5. `c079f60` phase-17
6. `34c4227` phase-18
7. `0a87007` phase-19
8. `ca1c03b` phase-20
9. `7c225ba` phase-21
10. `15c3188` phase-22
11. `922c84d` phase-23
12. `c5e6383` phase-24
13. `3c02cc2` phase-25
14. `8fdf9b6` P0/P1 稳定性批次
15. `e737158` moderation_views 语法/导入修复
16. `e59898f` 测试门禁（legacy/e2e 显式化）
17. `541cdfc` 后端 smoke 套件
18. `8e3f8af` 后端核心回归（10）
19. `3390d46` 后端扩展回归（12）
20. `67b6f60` 认证/审核回归（12）
21. `a72ea8e` 运维回归（15）+ logger 修复
22. `2616c8f` 发布门禁脚本与矩阵

## 三、关键回滚点（按功能）

- UI Phase 13~25 终点：`3c02cc2`
- P0/P1 修复锚点：`8fdf9b6`
- API 修复补丁：`e737158`
- 测试体系锚点：`a72ea8e`
- 发布门禁锚点：`2616c8f`

## 四、出问题时回滚策略

1. 快速回退到 UI 完成态（不含测试体系）：
```bash
git reset --hard 3c02cc2
```

2. 保留稳定性修复，去掉测试扩展：
```bash
git reset --hard e737158
```

3. 回到最完整最新状态：
```bash
git reset --hard 2616c8f
```

> 注意：`reset --hard` 会丢弃未提交改动，执行前先 `git status` 确认。
